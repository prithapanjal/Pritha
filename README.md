# Pritha Chatbot

A simple React + Vite chatbot app. The user can ask a question, and the robot replies step by step.

## Run Locally

```bash
npm install
npm.cmd run dev
```

Then open the local Vite URL in your browser, usually:

```text
http://localhost:5173/
```
